name: CI
on: 
    push:
jobs: 
  build-test:
    runs-on: ubuntu-latest
    steps:
    - name: Accedemos al reposotorio
    - uses: actions/checkout@v3
    - uses: php-actions/composer@v6
    uses: php-actions/phpunit@master
    with:
          bootstrap: vendor/autoload.php
          configuration: phpunit.xml
